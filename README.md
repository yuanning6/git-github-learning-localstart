# Learn Git & GitHub local start

`git remote add origin [repo link]` add remote repo.

`git remote -v` see remote repo.

`git push -u origin main` -u: upstream, push to by default, use `git push` in the future.